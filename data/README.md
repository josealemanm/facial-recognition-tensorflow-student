# Data layout

Place authorized images into raw/train/identity and raw/test/identity. The
preprocessing command writes normalized images into matching folders under
processed.

Everything below raw and processed is ignored by Git except the placeholder
files. Do not remove those ignore rules when using human face images.
