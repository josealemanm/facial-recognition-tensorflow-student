# Security policy

This educational project does not provide production security guarantees.

- Never commit face images, embeddings, trained classifiers, or private paths.
- Treat `.joblib` files as executable pickle data; load only trusted artifacts.
- Pin and review dependencies before deployment.
- Keep any research dataset encrypted and access-controlled outside Git.
- Report a repository-specific vulnerability through a private security
  advisory rather than a public issue.
